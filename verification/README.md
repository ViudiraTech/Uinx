# Verification Workspace Policy

This directory intentionally contains no checked-in test transcripts.

Use `docs/VERIFICATION.md` for the reproducible commands. Local/CI build logs, temporary probe programs, benchmark output, and CTest transcripts are disposable artifacts and should be removed after verification rather than accumulated in the source tree.
